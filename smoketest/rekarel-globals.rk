import rekarel.globals;

class program {
    program () {
        iterate (beepersInBeeperBag) {
            putbeeper();
        }

        if (true) {
            turnleft();
        }

        turnoff();
    }
}
